{
  "name": "@solana-playground/db",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.0",
    "postgres": "^3.4.3"
  },
  "devDependencies": {
    "@solana-playground/config-tsconfig": "workspace:*",
    "@types/node": "^20.11.0",
    "drizzle-kit": "^0.20.6",
    "typescript": "^5.3.3"
  }
}

